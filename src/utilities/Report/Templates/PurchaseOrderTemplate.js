import { PDFTools } from "../PDFTools"

export const purchaseOrderTemplate = (data) => {
    let document = {
        content: [
            'Content1',
            'Content2',
            'Content3',
            'Content4',

        ]
    }
    return document
}